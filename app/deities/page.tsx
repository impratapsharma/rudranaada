import type {Metadata} from 'next'; import Link from 'next/link'; import {deityHubs} from '@/lib/content'; import {Breadcrumbs} from '@/components/Breadcrumbs';
export const metadata:Metadata={title:'Deities',description:'Source-aware guides to deities and divine forms across Indian epic and devotional traditions.',alternates:{canonical:'/deities'}};
export default function Page(){return <><section className="pageHero"><div className="shell"><Breadcrumbs items={[{label:'Deities',href:'/deities'}]}/><div className="eyebrow">Guides</div><h1>Names, forms, stories and traditions.</h1><p>Each hub connects textual references, later traditions, symbolism, songs and related stories without pretending they are all the same layer.</p></div></section><section className="section"><div className="shell grid3">{deityHubs.map(d=><Link className="card" href={'/deities/'+d.slug} key={d.slug}><h3>{d.name}</h3><p>{d.description}</p></Link>)}</div></section></>}
