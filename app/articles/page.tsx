import type {Metadata} from 'next'; import Link from 'next/link'; import {articles} from '@/lib/content'; import {Breadcrumbs} from '@/components/Breadcrumbs';
export const metadata:Metadata={title:'Stories & Essays',description:'Research-backed stories and essays on Indian epics, deities, devotional traditions and philosophy.',alternates:{canonical:'/articles'}};
export default function Page(){return <><section className="pageHero"><div className="shell"><Breadcrumbs items={[{label:'Stories',href:'/articles'}]}/><div className="eyebrow">Stories & essays</div><h1>Go deeper than the song.</h1><p>Source-aware writing on epics, devotional traditions, characters, philosophy and the worlds behind RudraNāda music.</p></div></section><section className="section"><div className="shell">{articles.map(a=><Link className="listLink" href={'/articles/'+a.slug} key={a.slug}><div><div className="cardMeta">{a.category} · {a.readingMinutes} min</div><h3>{a.title}</h3><p>{a.dek}</p></div><span>→</span></Link>)}</div></section></>}
